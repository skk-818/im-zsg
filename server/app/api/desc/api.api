syntax = "v1"

service api {
	@handler ping
	post /ping
}

type (
	AuthLoginReq {
		Username string
		Password string
	}
	AuthLoginReply {
		Token         string
		Authorization string
	}
)

service api {
	@handler authLogin
	post /authLogin (AuthLoginReq) returns (AuthLoginReply)
}

